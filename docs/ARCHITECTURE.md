# 架构文档

> 占位，将在 **T15 集成测试 + 文档定稿** 阶段完整化。

## 模块图

```
�──────────────────────────┐      ┌────────────────────────────┐
│  Browser (React SPA)     │ HTTP │  Backend (Go + Fiber)      │
│  - 路由 / 状态 / UI      │ ───► │  - REST API (/api/...)     │
│  - 虚拟滚动 / 主题       │ SSE  │  - 扫描服务 + 缓存         │
│  - 查看器 / 幻灯片       │ ◄─── │  - 偏好持久化 (JSON)       │
└──────────────────────────┘      └────────────────────────────┘
                                            │
                                            ▼
                                  ┌──────────────────────────┐
                                  │  文件系统 (漫画根目录)    │
                                  │  E:\漫画 → ./comics      │
                                  └──────────────────────────┘
```

## 关键流程

- 启动 → 配置加载 → 健康检查 → 扫描
- 扫描 → SSE 进度 → 内存 + 磁盘缓存
- 查看 → 缩略图懒加载 → 图片流式传输（Range）

---

详细模块说明、序列图、部署架构将在 T15 完成。
