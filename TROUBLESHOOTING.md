# 故障排除指南

## 当前状态

✅ **主进程启动成功**
- Electron 窗口正常创建
- 窗口尺寸: 1200x800
- 窗口位置: 居中显示
- 页面加载完成

✅ **Vite 开发服务器正常运行**
- 地址: http://localhost:9999
- 热重载已启用

## 如果仍然白屏

### 1. 检查开发者工具
- 应用启动时会自动打开开发者工具
- 查看 Console 选项卡是否有错误信息
- 如果有 ErrorBoundary 捕获的错误，会显示详细错误页面

### 2. 访问 Web 版本测试
```bash
# 在浏览器中访问
http://localhost:9999
```
这可以绕过 Electron，直接在浏览器中测试前端代码。

### 3. 查看网络请求
- 打开浏览器开发者工具（F12）
- 切换到 Network 选项卡
- 刷新页面，查看是否有资源加载失败（红色条目）

### 4. 常见错误

#### 4.1 组件导入错误
如果某个 React 组件导入失败，错误边界会显示：
- 错误信息
- 堆栈跟踪
- 重新加载按钮

#### 4.2 样式问题
如果 Tailwind CSS 未正确加载：
- 页面会有基本样式（ErrorBoundary 使用内联样式）
- 自定义类名可能不生效

#### 4.3 异步数据加载错误
如果 Zustand store 或数据管理器出错：
- 错误信息会显示在页面上
- 可以点击"重试"按钮

## 解决方案

### 方法 1: 使用最小化版本
临时使用测试版本确认基础功能：
```typescript
// src/renderer/main.tsx
// import App from './App'
// import MinimalApp from './App.minimal'
<MinimalApp />
```

### 方法 2: 重新构建
```bash
npm run build
npx electron .
```

### 方法 3: 检查日志
应用启动时会输出详细日志，包括：
- 窗口创建和显示
- 页面加载状态
- 任何错误信息

## 下一步
如果问题仍然存在，请提供：
1. 错误边界页面截图（如果有）
2. 浏览器开发者工具 Console 错误信息
3. 运行 `npm run dev` 的完整输出日志
